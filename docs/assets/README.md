# Documentation assets

`workspace-overview.png` is a capture of the real browser UI using the repository's synthetic demo fixture. It contains no real API request, credential, endpoint, personal path, or private project data.
